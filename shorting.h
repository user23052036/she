#ifndef SHORTING_H// If shorting.h is not included to any file
#define SHORTING_H// Declare macro as a flag that specifies shorting.h is included

void bubble_a(int*,int);
void bubble_d(int*,int);
void insertion_a(int*,int);
void insertion_d(int*,int);
void selection_a(int*,int);
void selection_d(int*,int);

#endif