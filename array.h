#ifndef ARRAY_H// If shorting.h is not included to any file
#define ARRAY_H// Declare macro as a flag that specifies shorting.h is included

void insert(int*,int,int,int);
void del(int*,int,int);
int l_search(int*,int,int);
int if_pallindrome(int*,int);
int* merge(int*,int*,int*,int,int);

#endif